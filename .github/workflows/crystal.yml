name: Crystal CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: crystallang/crystal:1.6.2-alpine-build

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: make
